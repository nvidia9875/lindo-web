# 作品画像フォルダ

ここに作品画像を置きます。

- 形式：WebP または AVIF 推奨（JPG可）
- 横幅：1600px 程度を目安（重すぎる元画像はアップしない）
- カバー画像（一覧サムネ＆SNSシェア画像）：横長 16:10〜16:9 が安定
- ファイル名：半角英数・ハイフン（例：`artistx-cover.webp`）

`../works.json` の各作品の `cover` / `gallery` から、ここのファイルを
`images/ファイル名` で参照します。デモでは外部プレースホルダ画像を使用しています。
