# LINDO — robots.txt
#
# 配信しているのは1ページ構成のトップのみ（scripts/build-site.sh が dist/ に
# 集めたものだけが公開される）。旧デモ（concepts/ works/ gallery.html）と
# 内部ドキュメントは配信対象から外してあるので、Disallow で隠す必要は無い。
#
# 公開前は HTML 側の <meta name="robots" content="noindex,nofollow"> で
# インデックスを止めている（microCMS「サイト設定」の「検索エンジンに載せない」）。
# 公開日にそのスイッチを OFF にする。ここは触らない。

User-agent: *
Allow: /

Sitemap: https://styledbylindo.com/sitemap.xml
